generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
// ... Todos modelos conforme requisitos Apontador (Obra, Viagem, Veiculo, Profissional, Usuario, Permissao, EmpresaConfig, Ocorrencias, ClimaTempo) ...